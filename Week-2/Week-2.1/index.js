/* const { log } = require("console");
const fs = require("fs"); // external libraries

const data = fs.readFileSync("a.txt", "utf8"); // utf is encoding which undersrood by humans ( eng lang )
console.log(data);

const data2 = fs.readFileSync("b.txt", "utf8"); 
console.log(data2);
 */

