"use strict";
// trial
let studentRec = (stu) => {
    console.log(`student roll no is : ${stu.rollno} and student name is ${stu.name}`);
};
// so with this in a cleaner way we have selected the fields that we only want rather than passing many arg again and again.
